name: Repair Billing Runtime Secrets

# Deprecated: runtime billing secrets are now synchronized by the primary
# deployment workflow. Keep this file inert so historical links remain valid.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Deprecated workflow; use Deploy Momentum Stack.'
